git clone https://github.com/Jobbetter666/blog-ca-nhan.git
cd blog-ca-nhan
 echo "<!DOCTYPE html> ... </html>" > index.html
echo "body { ... }" > styles.css
git add .
git commit -m "Initial commit"
git push origin main
